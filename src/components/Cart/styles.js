import styled from 'styled-components';

const MinicartWrapper = styled.div`
  background-color: red;
`;
const CartPageWrapper = styled.div`
  text-align: center;
`;

export { MinicartWrapper, CartPageWrapper };
